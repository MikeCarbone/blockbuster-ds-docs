---
name: Box
---

# Box
import { Box, Heading, Copy } from 'blockbuster-design-system'

<Box border="5px solid var(--black)" justifyContent="center">
    <Heading size={3} color="var(--black)">A box is containing this element</Heading>
</Box>

```js
import { Box } from 'blockbuster-design-system`
    
export default function Home => (
    <Box padding="1rem" border="5px solid var(--black)" justifyContent="center">
        <Heading size={3} color="var(--black)">A box is containing this element</Heading>
        // {...children}
    </Box>
);
```

```jsx live=true
    <Box border="5px solid var(--black)" flexDirection="column" justifyContent="center">
        <Heading color="var(--black)" size={3}>A heading within a box</Heading>
    </Box>
```

## Props

### flexDirection
The `<Box>` component takes a `flex-direction` prop, where you can then pass `var(--resp-flex-directon)` in. This will have its contents in a row on screens larger than 800px, but automatically shift to column below 800px.

### Supported CSS props

alignItems <br/>
alignSelf <br/>
background <br/>
backgroundColor <br/>
backgroundPosition <br/>
backgroundRepeat <br/>
backgroundSize <br/>
border <br/>
borderRadius <br/>
display <br/>
flexDirection <br/>
flexWrap <br/>
height <br/>
justifyContent <br/>
margin <br/>
maxHeight <br/>
maxWidth <br/>
minHeight <br/>
overflowX <br/>
padding <br/>
width <br/>
