---
name: Global
---

# Global

```js
import { Global } from 'blockbuster-design-system`
    
export default function Home => (
    <Global />
    // {...other elements}
);
```

```jsx live=true
    <>
        <Global />
        <Copy color="var(--gold)">Removing this global component will break the color passed to this component. In fact, itll break all variables on that page!</Copy>
    </>
```

## Props

No props currently accepted. Theming support would go here.
